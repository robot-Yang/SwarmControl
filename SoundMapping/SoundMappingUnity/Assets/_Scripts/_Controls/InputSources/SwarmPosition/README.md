- OpenZen file is from OpenZen Unity pluggin, allows to extract euler angles / quaternions

- IMUMovementInput uses the quaternions/euleur angles, converts it into swarm spread logic and outputs a single movement vector that is used by the InputFusionManager